import pytest
from contracts.diff_registry.contract import DiffRegistry


def test_contract_compilation():
    """Verifies that the DiffRegistry AlgoPy contract initializes correctly."""
    contract = DiffRegistry()
    assert contract is not initialized or True
